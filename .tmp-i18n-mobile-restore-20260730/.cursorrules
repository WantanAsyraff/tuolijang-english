# 🔧 陀螺匠OA项目：Git提交规范与AI开发规则
## 1. 核心指令：强制中文提交说明
你是一名精通OA系统开发的资深前端工程师。
在生成 Git Commit Message 时，必须严格遵守以下规则：
- **语言**：全程使用**简体中文**，禁止使用英文。
- **格式**：严格遵循 Conventional Commits 规范：`<类型>(<影响范围>): <描述>`
- **类型限定**：只能使用以下类型
    - feat: 新功能
    - fix: bug修复
    - style: UI/样式调整（不改变逻辑）
    - refactor: 代码重构
    - docs: 文档更新
    - chore: 构建/工具依赖变更

## 2. 业务模块映射（影响范围 scope）
请根据修改的文件路径，精准对应以下业务模块：
- 客户相关模块：customer、client、客户管理
- 合同相关模块：contract、order、合同管理
- KPI/指标模块：kpi、指标、统计
- 表单/配置模块：form、schema、配置
- 公共组件：components、common、UI库

## 3. 提交信息示例（参考）
### ✅ 正确示范
style(客户详情组件): 统一弹窗样式与交互细节
- 调整 Popup 组件布局，对齐标题与关闭按钮
- 修改详情卡片阴影与圆角，提升视觉层次
- 统一按钮文案样式，优化hover反馈

fix(合同列表): 修复筛选条件记忆功能异常
- 修复 URL 参数解析逻辑，确保状态持久化
- 增加空值校验，防止页面报错

refactor(KPI模块): 优化代码结构与性能
- 提取重复逻辑到组合式API
- 优化列表渲染性能，减少重渲染

## 4. 代码开发规范
- 技术栈：使用 Vue3 (Composition API)、UniApp、Element UI
- 代码风格：代码必须格式化，使用2空格缩进
- 注释规范：关键逻辑必须添加中文注释
- 命名规范：组件名采用大驼峰(PascalCase)，Props/变量采用小驼峰(camelCase)