---
id: tuoluojiang-request-validation
trigger: "when creating new API endpoints that accept input"
confidence: 0.9
domain: validation
source: local-repo-analysis
---

# 使用 FormRequest 进行验证

## Action
创建专用的 FormRequest 类进行参数验证：

```php
// app/Http/Requests/UserRequest.php
class UserRequest extends FormRequest
{
    public function rules(): array
    {
        $rules = [
            'name' => 'required|string|max:255',
            'email' => 'required|email|unique:users,email',
            'phone' => 'nullable|regex:/^1[3-9]\d{9}$/',
        ];

        // 根据场景调整规则
        if ($this->isMethod('PUT')) {
            $rules['email'] = 'required|email|unique:users,email,' . $this->id;
        }

        return $rules;
    }

    public function messages(): array
    {
        return [
            'name.required' => '用户名不能为空',
            'email.email' => '邮箱格式不正确',
        ];
    }
}

// 在控制器中使用
public function store(UserRequest $request)
{
    $validated = $request->validated();
    // ...
}
```

## Rules
1. 所有用户输入必须验证
2. 使用 FormRequest 类而非控制器内验证
3. 验证规则按场景区分（创建/更新）
4. 提供清晰的中文错误消息
