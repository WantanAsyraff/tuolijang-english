// PC popup size tokens. Keep dialog and drawer sizes centralized for new pages.

export const DIALOG_SIZE = Object.freeze({
  XS: '470px',
  SM: '540px',
  MD: '650px',
  LG: '800px',
  XL: '1000px'
})

export const DRAWER_SIZE = Object.freeze({
  SM: '600px',
  MD: '800px',
  LG: '1000px',
  XL: '1200px'
})

export const DIALOG_SIZE_LIST = Object.freeze(Object.values(DIALOG_SIZE))
export const DRAWER_SIZE_LIST = Object.freeze(Object.values(DRAWER_SIZE))
